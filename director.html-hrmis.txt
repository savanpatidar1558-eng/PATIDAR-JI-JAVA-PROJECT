<!DOCTYPE html>
<html lang="hi">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Director Dashboard - Premium</title>
  <style>
    /* 🌟 प्रीमियम बेस थीम */
    body { font-family: 'Segoe UI', Roboto, Helvetica, Arial, sans-serif; background: #f0f4f8; margin: 0; padding: 0; color: #2d3436; }
    
    /* 🌟 रॉयल टॉप नेविगेशन */
    .top-navbar { background: linear-gradient(135deg, #1e3c72 0%, #2a5298 100%); color: white; padding: 15px 25px; display: flex; justify-content: space-between; align-items: center; box-shadow: 0 4px 15px rgba(0,0,0,0.15); position: sticky; top: 0; z-index: 1000; }
    .top-navbar h2 { margin: 0; font-size: 22px; letter-spacing: 1px; font-weight: 800; text-shadow: 1px 1px 3px rgba(0,0,0,0.3);}
    .home-btn { background: rgba(255,255,255,0.2); color: white; border: 1px solid rgba(255,255,255,0.4); padding: 8px 18px; border-radius: 20px; font-weight: bold; cursor: pointer; font-size: 14px; transition: 0.3s; backdrop-filter: blur(5px);}
    .home-btn:hover { background: white; color: #1e3c72; }

    /* 🌟 मॉडर्न ग्रिड लेआउट */
    .dashboard-container { padding: 30px 20px; max-width: 1250px; margin: 0 auto; display: grid; grid-template-columns: repeat(auto-fit, minmax(340px, 1fr)); gap: 25px; }
    
    /* 🌟 3D कार्ड डिज़ाइन */
    .card { background: white; padding: 30px 25px; border-radius: 16px; box-shadow: 0 8px 25px rgba(0,0,0,0.06); transition: transform 0.3s ease, box-shadow 0.3s ease; border-top: 6px solid #3498db; position: relative;}
    .card:hover { transform: translateY(-5px); box-shadow: 0 15px 35px rgba(0,0,0,0.1); }
    .card h3 { margin-top: 0; border-bottom: 2px solid #f1f2f6; padding-bottom: 12px; color: #2c3e50; font-size: 18px; display: flex; align-items: center; gap: 10px;}

    /* 🌟 स्टेटिस्टिक्स बॉक्स */
    .stats-row { display: flex; justify-content: space-between; margin-bottom: 20px; gap: 12px;}
    .stat-box { flex: 1; padding: 15px 10px; border-radius: 12px; color: white; text-align: center; box-shadow: 0 4px 10px rgba(0,0,0,0.1); transition: 0.3s;}
    .stat-box:hover { transform: scale(1.05); }
    .bg-green { background: linear-gradient(135deg, #00b09b, #96c93d); } 
    .bg-red { background: linear-gradient(135deg, #eb3349, #f45c43); } 
    .bg-orange { background: linear-gradient(135deg, #f1c40f, #e67e22); }
    .stat-box h2 { margin: 5px 0 0 0; font-size: 28px; font-weight: 900; }

    /* 🌟 लाइव लिस्ट */
    .live-list { background: #f8f9fa; border: 1px solid #e1e5eb; border-radius: 10px; max-height: 220px; overflow-y: auto; padding: 10px; }
    .live-list div { padding: 10px; border-bottom: 1px solid #ecf0f1; display: flex; justify-content: space-between; font-size: 14px; transition: 0.2s;}
    .live-list div:hover { background: #fff; box-shadow: 0 2px 5px rgba(0,0,0,0.05); border-radius: 5px;}
    .badge-present { color: #27ae60; font-weight: bold; background:#e8f8f5; padding:3px 8px; border-radius:12px; font-size:12px;} 
    .badge-absent { color: #c0392b; font-weight: bold; background:#fdedec; padding:3px 8px; border-radius:12px; font-size:12px;} 
    .badge-late { color: #d35400; font-weight: bold; background:#fef5e7; padding:3px 8px; border-radius:12px; font-size:12px;}

    /* 🌟 प्रीमियम फॉर्म इनपुट */
    .form-group { margin-bottom: 18px; }
    label { display: block; font-weight: 600; margin-bottom: 6px; color: #34495e; font-size: 13px; text-transform: uppercase; letter-spacing: 0.5px;}
    input, select, textarea { width: 100%; padding: 12px 15px; border: 2px solid #dcdde1; border-radius: 8px; font-size: 15px; box-sizing: border-box; background: #fdfdfd; transition: all 0.3s; color: #2d3436; font-family: inherit;}
    input:focus, select:focus, textarea:focus { border-color: #3498db; background: #fff; box-shadow: 0 0 0 4px rgba(52, 152, 219, 0.15); outline: none; }
    
    /* 🌟 सुपर बटन्स */
    button.action-btn { width: 100%; padding: 14px; background: linear-gradient(135deg, #3498db, #2980b9); color: white; border: none; border-radius: 8px; font-size: 16px; font-weight: bold; cursor: pointer; transition: 0.3s; box-shadow: 0 4px 15px rgba(52, 152, 219, 0.3); letter-spacing: 1px;}
    button.action-btn:hover { background: linear-gradient(135deg, #2980b9, #1f6391); transform: translateY(-2px); box-shadow: 0 6px 20px rgba(52, 152, 219, 0.4); }
    button.action-btn:active { transform: translateY(0); }

    /* 🌟 पेरोल टेबल (Code.gs से आने वाले टेबल का डिज़ाइन) */
    table { width: 100%; border-collapse: collapse; font-size: 14px; background: white; border-radius: 8px; overflow: hidden; box-shadow: 0 0 10px rgba(0,0,0,0.05); margin-top:10px; }
    th { background: #1e3c72 !important; color: white !important; padding: 12px; text-align: left; border:none !important;}
    td { padding: 12px; border-bottom: 1px solid #ecf0f1; border-right: none !important; border-left: none !important; vertical-align: middle; }
    tr:hover { background-color: #f8f9fa; }
  </style>
</head>
<body>
  
  <div class="top-navbar">
    <h2>👑 माँ अंबिका डायरेक्टर</h2>
    <button class="home-btn" onclick="goHome()">🏠 होम पेज</button>
  </div>

  <div class="dashboard-container">
    <div class="card" style="border-top-color: #27ae60;">
      <h3>📊 आज की हाजिरी (Live)</h3>
      <div class="stats-row">
        <div class="stat-box bg-green"><span style="font-size:12px; opacity:0.9;">Present</span><h2 id="totalPresent">0</h2></div>
        <div class="stat-box bg-red"><span style="font-size:12px; opacity:0.9;">Absent</span><h2 id="totalAbsent">0</h2></div>
        <div class="stat-box bg-orange"><span style="font-size:12px; opacity:0.9;">Late</span><h2 id="totalLate">0</h2></div>
      </div>
      <div class="live-list" id="liveListDetails">⏳ डेटा लोड हो रहा है...</div>
    </div>

    <div class="card" style="border-top-color: #e74c3c;">
      <h3>📢 डिजिटल नोटिस बोर्ड</h3>
      <div class="form-group"><label>स्टाफ के लिए नया मैसेज:</label><textarea id="noticeMsg" rows="4" placeholder="जैसे: कल सुबह 10 बजे मीटिंग है..."></textarea></div>
      <button class="action-btn" style="background: linear-gradient(135deg, #e74c3c, #c0392b); box-shadow: 0 4px 15px rgba(231,76,60,0.3);" onclick="publishNotice()" id="btnNotice">🚀 नोटिस पब्लिश करें</button>
    </div>

    <div class="card" style="border-top-color: #f39c12; grid-row: span 2;">
      <h3>👤 नया स्टाफ रजिस्टर करें</h3>
      <form id="staffForm" onsubmit="submitForm(event)">
        <div class="form-group"><label>स्टाफ का पूरा नाम</label><input type="text" id="name" placeholder="जैसे: राहुल शर्मा" required></div>
        
        <div style="display:flex; gap:15px;">
            <div class="form-group" style="flex:1;"><label>पद (Designation)</label><input type="text" id="designation" placeholder="जैसे: X-Ray Tech" required></div>
            <div class="form-group" style="flex:1;"><label>वेतन (₹)</label><input type="number" id="salary" placeholder="जैसे: 15000" required></div>
        </div>

        <div style="display:flex; gap:15px;">
            <div class="form-group" style="flex:1;"><label>In Time (आने का समय)</label><input type="time" id="shiftIn" required value="10:00"></div>
            <div class="form-group" style="flex:1;"><label>Out Time (जाने का समय)</label><input type="time" id="shiftOut" required value="18:00"></div>
        </div>

        <div style="display:flex; gap:15px;">
            <div class="form-group" style="flex:1;"><label>मोबाइल नंबर</label><input type="tel" id="mobile" pattern="[0-9]{10}" placeholder="10 अंकों का नंबर" required></div>
            <div class="form-group" style="flex:1;"><label>जन्म तिथि (DOB)</label><input type="date" id="dob" required></div>
        </div>

        <div class="form-group"><label>ईमेल आईडी</label><input type="email" id="email" placeholder="सक्रिय ईमेल आईडी" required></div>

        <div class="form-group">
            <label>दस्तावेज़ (Aadhar / PAN / Photo)</label>
            <input type="file" id="docFile" accept="image/*,.pdf" style="border: 2px dashed #bdc3c7; background: #fafafa; padding: 15px; cursor: pointer; color:#7f8c8d;" required>
            <span style="font-size:11px; color:#95a5a6; display:block; margin-top:5px;">यह फाइल सुरक्षित ड्राइव में सेव हो जाएगी।</span>
        </div>

        <button type="submit" class="action-btn" id="submitBtn">सेव करें और एग्रीमेंट भेजें</button>
        <div id="message" style="margin-top:15px; text-align:center; font-weight:bold; font-size:14px;"></div>
      </form>
    </div>

    <div id="director-plugin-container" style="display: contents;"></div>
  </div>

  <script>
    // ==========================================
    // ⚠️ कोर फंक्शन (बिना किसी बदलाव के)
    // ==========================================
    var appUrl = ""; google.script.run.withSuccessHandler(function(url) { appUrl = url; }).getScriptUrl();
    function goHome() { if(appUrl) window.top.location.href = appUrl; }
    var pluginOnLoadTasks = []; 

    window.onload = function() {
      google.script.run.withSuccessHandler(function(stats) {
        document.getElementById('totalPresent').innerText = stats.present; document.getElementById('totalAbsent').innerText = stats.absent; document.getElementById('totalLate').innerText = stats.late;
        var listHtml = "";
        stats.list.forEach(function(item) {
          var badgeClass = item.status.includes("Absent") ? "badge-absent" : (item.status.includes("Late") ? "badge-late" : "badge-present");
          listHtml += "<div><span><b style='color:#2c3e50;'>" + item.name + "</b> <span style='color:#7f8c8d; font-size:12px;'>(" + item.inTime + ")</span></span> <span class='" + badgeClass + "'>" + item.status + "</span></div>";
        });
        if(stats.list.length === 0) listHtml = "<div style='justify-content:center; color:#7f8c8d;'>कोई डेटा नहीं मिला।</div>";
        document.getElementById('liveListDetails').innerHTML = listHtml;
      }).getTodayStats();

      google.script.run.withSuccessHandler(function(notice) { document.getElementById('noticeMsg').value = notice; }).getNotice();
      pluginOnLoadTasks.forEach(function(task) { task(); });
    };

    function publishNotice() {
       var msg = document.getElementById('noticeMsg').value; document.getElementById('btnNotice').innerText = "⏳ पब्लिश हो रहा है...";
       google.script.run.withSuccessHandler(function(res) { alert(res); document.getElementById('btnNotice').innerText = "🚀 नोटिस पब्लिश करें"; }).saveNotice(msg);
    }

    function formatTimeAMPM(time24) {
       var parts = time24.split(':');
       var h = parseInt(parts[0], 10);
       var m = parts[1];
       var ampm = h >= 12 ? 'PM' : 'AM';
       h = h % 12; h = h ? h : 12; 
       return h + ':' + m + ' ' + ampm;
    }

    function submitForm(event) {
      event.preventDefault(); 
      document.getElementById('submitBtn').innerText = "⏳ डेटा और दस्तावेज़ अपलोड हो रहा है..."; 
      document.getElementById('submitBtn').disabled = true;
      document.getElementById('message').innerText = "";
      
      var fileInput = document.getElementById('docFile');
      var file = fileInput.files[0];
      var shiftIn = document.getElementById('shiftIn').value;
      var shiftOut = document.getElementById('shiftOut').value;
      var finalShift = formatTimeAMPM(shiftIn) + " से " + formatTimeAMPM(shiftOut);

      if(file) {
        var reader = new FileReader();
        reader.onload = function(e) {
          var base64Data = e.target.result.split(',')[1];
          var staffData = { 
            name: document.getElementById('name').value, 
            designation: document.getElementById('designation').value, 
            shiftTime: finalShift, 
            salary: document.getElementById('salary').value, 
            dob: document.getElementById('dob').value, 
            email: document.getElementById('email').value, 
            mobile: document.getElementById('mobile').value,
            fileName: file.name,
            mimeType: file.type,
            fileData: base64Data
          };
          
          google.script.run.withSuccessHandler(function(response) {
            if(response == "Success") { 
              document.getElementById('message').innerHTML = "✅ सफलतापूर्वक रजिस्टर हो गया!<br><span style='font-size:12px; font-weight:normal;'>एग्रीमेंट ईमेल पर भेज दिया गया है।</span>"; 
              document.getElementById('message').style.color = "#27ae60"; 
              document.getElementById('staffForm').reset(); 
            } else { 
              document.getElementById('message').innerText = response; 
              document.getElementById('message').style.color = "#c0392b"; 
            }
            document.getElementById('submitBtn').innerText = "सेव करें और एग्रीमेंट भेजें"; 
            document.getElementById('submitBtn').disabled = false;
          }).saveNewStaff(staffData);
        };
        reader.readAsDataURL(file);
      }
    }
  </script>

  <script>
    var leaveHtml = '<div class="card" style="border-top-color: #9b59b6;"><h3>📩 छुट्टी की रिक्वेस्ट</h3><div id="pendingLeavesList" style="background: #f8f9fa; padding: 10px; border-radius: 10px; max-height: 250px; overflow-y: auto; font-size: 14px; border:1px solid #e1e5eb;">⏳ डेटा लोड हो रहा है...</div></div>';
    document.getElementById('director-plugin-container').innerHTML += leaveHtml;
    function loadPendingLeaves() {
      google.script.run.withSuccessHandler(function(leaves) {
        var listDiv = document.getElementById('pendingLeavesList');
        if(leaves.length === 0) { listDiv.innerHTML = "<p style='text-align:center; color:#27ae60; font-weight:bold; margin-top:20px;'>🎉 कोई पेंडिंग रिक्वेस्ट नहीं है!</p>"; return; }
        var html = "";
        leaves.forEach(function(leave) {
          html += "<div style='background:white; padding:15px; margin-bottom:12px; border-radius:10px; border-left:5px solid #9b59b6; box-shadow:0 4px 10px rgba(0,0,0,0.05);'><b style='font-size:16px; color:#2c3e50;'>" + leave.name + "</b> <span style='font-size:12px; color:#7f8c8d; float:right;'>" + leave.leaveDate + "</span><br><div style='background:#fdf2e9; padding:8px; border-radius:6px; margin-top:8px; color:#d35400; font-size:13px;'><b>कारण:</b> " + leave.reason + "</div><input type='text' id='remark_" + leave.row + "' placeholder='रिमार्क लिखें (वैकल्पिक)' style='width:100%; padding:10px; margin-top:10px; margin-bottom:10px; border:1px solid #dcdde1; border-radius:6px;'><div style='display:flex; gap:10px;'><button type='button' onclick='actionLeave(" + leave.row + ", \"Approve\", \"" + leave.name + "\")' style='flex:1; background:linear-gradient(135deg, #27ae60, #2ecc71); padding:10px; font-weight:bold; color:white; border:none; border-radius:6px; cursor:pointer;'>✅ Approve</button><button type='button' onclick='actionLeave(" + leave.row + ", \"Reject\", \"" + leave.name + "\")' style='flex:1; background:linear-gradient(135deg, #e74c3c, #c0392b); padding:10px; font-weight:bold; color:white; border:none; border-radius:6px; cursor:pointer;'>❌ Reject</button></div></div>";
        });
        listDiv.innerHTML = html;
      }).getPendingLeaves();
    }
    function actionLeave(rowNumber, status, staffName) {
      var remark = document.getElementById('remark_' + rowNumber).value;
      if(!confirm("क्या आप सच में स्टेटस अपडेट करना चाहते हैं?")) return;
      document.getElementById('pendingLeavesList').innerHTML = "<div style='text-align:center; padding:20px; font-weight:bold;'>⏳ अपडेट हो रहा है...</div>";
      google.script.run.withSuccessHandler(function(res) { alert(res); loadPendingLeaves(); }).processLeave(rowNumber, status, remark, staffName);
    }
    pluginOnLoadTasks.push(loadPendingLeaves);
  </script>

  <script>
    var advHtml = '<div class="card" style="border-top-color: #8e44ad;"><h3>💳 स्टाफ एडवांस पेमेंट</h3><div class="form-group"><label>स्टाफ का नाम</label><select id="advStaffName"><option value="">लोड हो रहा है...</option></select></div><div style="display:flex; gap:15px;"><div class="form-group" style="flex:1;"><label>तारीख</label><input type="date" id="advDate" required></div><div class="form-group" style="flex:1;"><label>रकम (₹)</label><input type="number" id="advAmount" placeholder="जैसे: 2000" required></div></div><div class="form-group"><label>कारण / रिमार्क</label><input type="text" id="advReason" placeholder="जैसे: घर खर्च" required></div><button type="button" class="action-btn" onclick="submitAdvance()" style="background: linear-gradient(135deg, #8e44ad, #9b59b6); box-shadow: 0 4px 15px rgba(142,68,173,0.3);">पैसे सेव करें</button></div> <div class="card" style="border-top-color: #f1c40f;"><h3>📊 रिपोर्ट डाउनलोड</h3><div style="display:flex; flex-direction:column; gap:15px; margin-top:15px;"><a id="btnAttExcel" href="#" target="_blank" style="text-decoration:none; background:linear-gradient(135deg, #27ae60, #2ecc71); color:white; padding:14px; border-radius:8px; text-align:center; font-weight:bold; box-shadow:0 4px 10px rgba(39,174,96,0.3); transition:0.3s;">📗 हाजिरी रजिस्टर (Excel)</a><a id="btnAttPdf" href="#" target="_blank" style="text-decoration:none; background:linear-gradient(135deg, #e74c3c, #c0392b); color:white; padding:14px; border-radius:8px; text-align:center; font-weight:bold; box-shadow:0 4px 10px rgba(231,76,60,0.3); transition:0.3s;">📕 हाजिरी रजिस्टर (PDF)</a><a id="btnAdvExcel" href="#" target="_blank" style="text-decoration:none; background:linear-gradient(135deg, #f39c12, #f1c40f); color:white; padding:14px; border-radius:8px; text-align:center; font-weight:bold; box-shadow:0 4px 10px rgba(243,156,18,0.3); transition:0.3s;">📒 एडवांस पेमेंट लेजर (Excel)</a></div></div>';
    document.getElementById('director-plugin-container').innerHTML += advHtml;
    function setupAdvanceAndReports() {
      document.getElementById("advDate").valueAsDate = new Date();
      google.script.run.withSuccessHandler(function(names) { var select = document.getElementById("advStaffName"); select.innerHTML = '<option value="">-- नाम चुनें --</option>'; names.forEach(function(name) { select.innerHTML += '<option value="' + name + '">' + name + '</option>'; }); }).getActiveStaffList();
      google.script.run.withSuccessHandler(function(links) { document.getElementById("btnAttExcel").href = links.attendanceExcel; document.getElementById("btnAttPdf").href = links.attendancePdf; document.getElementById("btnAdvExcel").href = links.advanceExcel; }).getReportLinks();
    }
    function submitAdvance() {
      var staffName = document.getElementById("advStaffName").value; var date = document.getElementById("advDate").value; var amount = document.getElementById("advAmount").value; var reason = document.getElementById("advReason").value;
      if(!staffName || !amount) { alert("⚠️ नाम और रकम भरें!"); return; }
      google.script.run.withSuccessHandler(function(res) { alert(res); document.getElementById("advAmount").value = ""; document.getElementById("advReason").value = ""; }).saveAdvancePayment(staffName, amount, date, reason);
    }
    pluginOnLoadTasks.push(setupAdvanceAndReports);
  </script>

  <script>
    var payrollHtml = `
    <div class="card" style="border-top-color: #34495e; grid-column: 1 / -1;">
      <h3 style="color: #34495e;">💸 स्मार्ट पेरोल (Salary Payout)</h3>
      <p style="font-size:13px; color:#7f8c8d; margin-top:-5px; margin-bottom:20px;">महीना चुनें। आप चाहें तो 'Pay' दबाने से पहले नेट सैलरी को एडिट भी कर सकते हैं।</p>
      
      <div style="display:flex; gap:15px; align-items:flex-end;">
          <div class="form-group" style="flex:1; margin-bottom:0;">
            <label>सैलरी का महीना (Month)</label>
            <input type="month" id="payrollMonth" required>
          </div>
          <button type="button" class="action-btn" onclick="loadPayroll()" style="flex:1; background: linear-gradient(135deg, #34495e, #2c3e50); box-shadow: 0 4px 15px rgba(44,62,80,0.3); margin-bottom:0;">सैलरी लिस्ट मंगाएं</button>
      </div>
      
      <div id="payrollContainer" style="margin-top: 20px; overflow-x: auto; display:none; padding-bottom:10px;"></div>
    </div>`;
    
    document.getElementById('director-plugin-container').innerHTML += payrollHtml;
    
    function setInitialPayrollMonth() { 
       var now = new Date(); var prevMonth = new Date(now.getFullYear(), now.getMonth() - 1, 1);
       var mInput = document.getElementById("payrollMonth"); 
       if(mInput) mInput.value = prevMonth.getFullYear() + "-" + (prevMonth.getMonth() + 1).toString().padStart(2, '0'); 
    }
    
    function loadPayroll() {
       var monthYear = document.getElementById("payrollMonth").value; 
       if(!monthYear) { alert("⚠️ महीना चुनें!"); return; }
       var reportDiv = document.getElementById("payrollContainer"); 
       reportDiv.style.display = "block"; reportDiv.innerHTML = "<div style='text-align:center; padding:20px; font-weight:bold; color:#3498db;'>⏳ सबकी हाजिरी और एडवांस का हिसाब लग रहा है... कृपया रुकें...</div>";
       
       google.script.run.withSuccessHandler(function(res) { 
           if(res.error) { reportDiv.innerHTML = "<span style='color:#e74c3c; font-weight:bold;'>❌ " + res.error + "</span>"; } 
           else { reportDiv.innerHTML = res.html; } 
       }).getMonthlySalaryData(monthYear);
    }

    function payStaffSalaryUI(staffName, monthYear, amount, email) {
       if(!confirm("क्या आप पक्का " + staffName + " को ₹" + amount + " सैलरी Pay करना चाहते हैं?")) return;
       document.getElementById("payrollContainer").innerHTML = "<div style='text-align:center; padding:20px; font-weight:bold; color:#27ae60;'>⏳ " + staffName + " का पेमेंट प्रोसेस हो रहा है और PDF बन रही है...</div>";
       google.script.run.withSuccessHandler(function(res) {
          if(res === "Success") {
             alert("✅ " + staffName + " की सैलरी सक्सेसफुल Pay हो गई!");
             loadPayroll(); 
          } else {
             alert("❌ एरर: " + res); loadPayroll();
          }
       }).processSalaryPayment(staffName, monthYear, amount, email);
    }

    pluginOnLoadTasks.push(setInitialPayrollMonth);
  </script>
</body>
</html>
<style>
  /* सिर्फ इस नए बॉक्स का डिज़ाइन (ताकि पुराना कुछ न बिगड़े) */
  .offer-card { background: white; border-radius: 12px; padding: 25px; box-shadow: 0 6px 15px rgba(0,0,0,0.1); border-top: 5px solid #8e44ad; margin: 20px auto; max-width: 800px; font-family: Arial, sans-serif;}
  .offer-title { font-size: 18px; color: #2c3e50; border-bottom: 2px solid #eee; padding-bottom: 10px; margin-top: 0; font-weight: 800; }
  .offer-alert { background: #e8f8f5; color: #27ae60; padding: 10px; border-radius: 6px; font-size: 13px; font-weight: bold; margin-bottom: 15px; border-left: 4px solid #27ae60; }
  .offer-form-group { margin-bottom: 15px; text-align: left;}
  .offer-form-group label { display: block; font-size: 13px; font-weight: bold; color: #555; margin-bottom: 5px; }
  .offer-form-group input { width: 100%; padding: 10px; border-radius: 6px; border: 1px solid #ccc; font-size: 14px; box-sizing: border-box; outline: none;}
  .offer-form-group input:focus { border-color: #8e44ad; }
  .offer-btn { width: 100%; padding: 12px; border: none; border-radius: 6px; font-size: 15px; font-weight: bold; color: white; background: #8e44ad; cursor: pointer; margin-top: 10px; transition: 0.3s;}
  .offer-btn:hover { background: #732d91; }
</style>

<div id="new-offer-module">
  <div class="offer-card">
    <h3 class="offer-title">📄 Send Offer Letter (Pre-Joining)</h3>
    <div class="offer-alert">💡 इसका इस्तेमाल तब करें जब कैंडिडेट को पुरानी जगह रिज़ाइन देने के लिए सिर्फ ऑफर लेटर चाहिए। इससे अटेंडेंस चालू नहीं होगी!</div>
    <form id="offerForm" onsubmit="handleOfferSubmit(event)">
      <div class="offer-form-group"><label>Candidate Name (पूरा नाम)</label><input type="text" id="oName" required></div>
      <div class="offer-form-group"><label>Designation (पद) (e.g. CT/MRI Tech)</label><input type="text" id="oRole" required></div>
      <div class="offer-form-group"><label>Offered Salary (मासिक वेतन - ₹)</label><input type="number" id="oSalary" required></div>
      <div class="offer-form-group"><label>Expected Joining Date (संभावित जॉइनिंग)</label><input type="date" id="oDate" required></div>
      <div class="offer-form-group"><label>Candidate Email (ऑफर लेटर भेजने के लिए)</label><input type="email" id="oEmail" required></div>
      <button type="submit" class="offer-btn">📨 SEND OFFER LETTER PDF</button>
    </form>
  </div>
</div>

<script>
  // यह स्क्रिप्ट सिर्फ ऑफर लेटर के लिए काम करेगी
  function handleOfferSubmit(e) {
    e.preventDefault();
    let name = document.getElementById("oName").value;
    let role = document.getElementById("oRole").value;
    let salary = document.getElementById("oSalary").value;
    let date = document.getElementById("oDate").value;
    let email = document.getElementById("oEmail").value;

    // पुराना लोडर चेक करना (अगर है तो यूज़ करें, वरना सिंपल अलर्ट)
    let loader = document.getElementById("loader");
    let loaderText = document.getElementById("loader-text");
    if(loader && loaderText) {
        loaderText.innerText = "ऑफर लेटर PDF जनरेट हो रहा है और ईमेल भेजा जा रहा है...";
        loader.style.display = "flex";
    } else {
        alert("⏳ प्रोसेसिंग... ऑफर लेटर भेजा जा रहा है। कृपया कुछ सेकंड प्रतीक्षा करें...");
    }

    google.script.run.withSuccessHandler(function(res) {
      if(loader) loader.style.display = "none";
      alert(res);
      if(res.includes("Success")) document.getElementById("offerForm").reset();
    }).sendOfferLetter(name, role, salary, date, email);
  }

  // स्मार्ट जावास्क्रिप्ट: इस बॉक्स को आपके पुराने डिज़ाइन के ठीक नीचे सेट करने के लिए
  document.addEventListener("DOMContentLoaded", function() {
      let mainContainer = document.querySelector(".container");
      let offerModule = document.getElementById("new-offer-module");
      if(mainContainer && offerModule) {
          mainContainer.appendChild(offerModule.firstElementChild);
          offerModule.remove();
      }
  });
</script>
